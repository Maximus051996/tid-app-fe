/**
 * Default environment — used by `ng serve` and the dev build.
 * Override at deploy time by replacing this file via fileReplacements
 * in angular.json (or by setting these values at build time).
 */
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:3001/api',
};
